# EduShare 资源分享系统完整部署文档
> 服务器IP：`156.239.236.13`
> 主域名：`easydowork.cn`
> 环境：Ubuntu 24.04
> 项目路径：`/root/LearningResourceLibrary`
> 部署日期：2026-07-04

## 一、服务器基础环境清单
### 1. 核心软件版本
1. Java 17：后端SpringBoot运行环境
2. MySQL 8.0：业务数据库
3. Node.js 20 + npm：Vue前端打包构建
4. Nginx：静态资源托管 + 反向代理 + HTTPS
5. Certbot：免费SSL证书自动签发/续期

### 2. 安全组放行端口
- TCP 80（HTTP）
- TCP 443（HTTPS）
- TCP 22（SSH远程连接）
- TCP 3306（MySQL，按需限制内网访问）

## 二、数据库 MySQL 部署配置
### 1. 数据库信息
- 数据库名：`edushare`
- 访问账号：`edushare`
- 数据库密码：`123456`（你配置的密码）
- 字符集：`utf8mb4`
- 排序规则：`utf8mb4_unicode_ci`

### 2. 初始化步骤
1. 创建数据库、授权账号
```sql
CREATE DATABASE edushare DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'edushare'@'localhost' IDENTIFIED BY '123456';
GRANT ALL PRIVILEGES ON edushare.* TO 'edushare'@'localhost';
FLUSH PRIVILEGES;
```
2. 执行初始化SQL脚本
```bash
mysql -u edushare -p edushare < /root/LearningResourceLibrary/src/main/resources/sql/init.sql
```
3. 验证数据表：`SHOW TABLES;`
已生成表：admin、announcement、category、download_log、keyword_rule、resource、resource_tag、tag

## 三、后端 SpringBoot 部署（Systemd 托管）
### 1. 项目配置文件
路径：`/root/LearningResourceLibrary/src/main/resources/application.properties`
关键配置：
```properties
# 数据库连接
spring.datasource.username=edushare
spring.datasource.password=123456
spring.datasource.url=jdbc:mysql://localhost:3306/edushare?useUnicode=true&characterEncoding=utf8mb4

# 文件上传路径（注意是短横线，代码用 @Value("${file.upload-path}") 读取）
file.upload-path=/root/LearningResourceLibrary/upload/
file.max-size=10485760

# 服务端口
server.port=8080
```

### 2. 打包命令
```bash
cd /root/LearningResourceLibrary
mvn clean package -DskipTests
```
打包产物：`target/EduShare-0.0.1-SNAPSHOT.jar`

### 3. Systemd 服务配置
文件路径：`/etc/systemd/system/edushare.service`
```ini
[Unit]
Description=EduShare Backend Service
After=network.target mysql.service redis.service

[Service]
Type=simple
User=root
WorkingDirectory=/root/LearningResourceLibrary/target
ExecStart=/usr/bin/java -Xms256m -Xmx512m -jar EduShare-0.0.1-SNAPSHOT.jar
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

### 4. 后端运维命令
```bash
# 启动/停止/重启
systemctl start edushare
systemctl stop edushare
systemctl restart edushare

# 查看运行状态
systemctl status edushare

# 实时查看日志
journalctl -u edushare -f

# 设置开机自启（已启用）
systemctl enable edushare
```

## 四、前端 Vue3 + Vite 部署
### 1. 项目路径
前端根目录：`/root/LearningResourceLibrary/frontend`
打包产物目录：`/root/LearningResourceLibrary/frontend/dist`

### 2. 安装依赖 & 打包
```bash
# 切换淘宝镜像加速
npm config set registry https://registry.npmmirror.com
# 安装依赖
npm install
# 生产打包
npm run build
```

### 3. 权限修复（打包报错 Permission denied）
```bash
chmod -R +x node_modules/.bin/
```

## 五、Nginx 反向代理 + HTTPS 配置
### 1. Nginx 运行用户修改（解决/root目录403）
文件：`/etc/nginx/nginx.conf`
首行修改：`user root;`

### 2. 站点配置文件
路径：`/etc/nginx/sites-available/edushare`
```nginx
server {
    listen 80;
    server_name 156.239.236.13 easydowork.cn www.easydowork.cn;
    # HTTP全部强制跳转HTTPS
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name 156.239.236.13 easydowork.cn www.easydowork.cn;

    # 允许上传最大20MB（Nginx默认1MB，超出会报413）
    client_max_body_size 20m;

    # Certbot自动生成ssl证书路径
    ssl_certificate /etc/letsencrypt/live/easydowork.cn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/easydowork.cn/privkey.pem;

    # 前端静态页面
    location / {
        root /root/LearningResourceLibrary/frontend/dist;
        index index.html;
        # Vue history模式刷新404修复
        try_files $uri $uri/ /index.html;
    }

    # 后端API代理
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 上传文件静态访问（单数，旧路径兼容）
    location /upload/ {
        alias /root/LearningResourceLibrary/upload/;
        expires 30d;
        autoindex off;
    }

    # 封面图片访问（复数带s，代码返回的URL路径）
    location /uploads/ {
        alias /root/LearningResourceLibrary/upload/;
        expires 30d;
        autoindex off;
    }
}
```

### 3. 站点启用 & 默认页面关闭
```bash
# 软链接启用站点
ln -s /etc/nginx/sites-available/edushare /etc/nginx/sites-enabled/
# 删除默认页面
unlink /etc/nginx/sites-enabled/default

# 校验配置 + 重载
nginx -t
systemctl reload nginx

# 开机自启（已启用）
systemctl enable nginx
```

## 六、HTTPS 免费SSL证书（Let’s Encrypt）
### 1. 安装工具
```bash
apt update
apt install certbot python3-certbot-nginx -y
```

### 2. 签发证书命令
```bash
certbot --nginx -d easydowork.cn -d www.easydowork.cn
```
交互选择：
1. 输入邮箱（接收到期提醒）
2. `Y` 同意服务条款
3. `N` 拒绝EFF邮件推送
4. 选择 `2` 强制HTTP跳转HTTPS

### 3. 证书自动续期定时任务
定时任务配置：`crontab -e`
```cron
# 每日凌晨3点自动检测续期，更新后重载Nginx
0 3 * * * certbot renew --quiet && systemctl reload nginx
```
验证定时任务：`crontab -l`

### 4. 手动测试续期
```bash
certbot renew --dry-run
```

## 七、域名 DNS 解析配置
域名：`easydowork.cn`
解析类型全部为A记录，记录值 `156.239.236.13`
1. `@` → easydowork.cn 裸域名访问
2. `www` → www.easydowork.cn 带www访问
TTL：600s（10分钟）

## 八、代码更新全流程（长期维护用）
### 方式1：Git 一键更新脚本（推荐）
1. 创建更新脚本
```bash
nano /root/update.sh
```
```bash
#!/bin/bash
echo "===== 开始更新后端 ====="
cd /root/LearningResourceLibrary
git stash                         # 暂存服务器本地修改
git pull origin master
git stash drop                    # 不需要保留本地修改，直接丢弃
mvn clean package -DskipTests
systemctl restart edushare
echo "后端重启完成"

echo "===== 开始更新前端 ====="
cd /root/LearningResourceLibrary/frontend
git stash
git pull origin master
git stash drop
npm install
npm run build
systemctl reload nginx
echo "前端更新完成，全站更新完毕"
```
2. 添加执行权限
```bash
chmod +x /root/update.sh
```
3. 后续更新只执行：
```bash
/root/update.sh
```

### 方式2：本地打包上传更新（无Git仓库）
1. 后端更新
本地打包jar → scp上传至服务器target目录 → `systemctl restart edushare`
2. 前端更新
本地`npm run build`得到dist → scp覆盖服务器frontend/dist → `systemctl reload nginx`

## 九、文件上传目录
上传存储路径：`/root/LearningResourceLibrary/upload/`
网页访问路径：`https://easydowork.cn/upload/xxx`

## 十、常用排查命令汇总
```bash
# 1. 后端服务
systemctl status edushare
journalctl -u edushare -f

# 2. Nginx
systemctl status nginx
nginx -t
systemctl reload nginx

# 3. 证书续期
certbot renew --dry-run
crontab -l

# 4. 数据库登录
mysql -u edushare -p

# 5. 查看定时任务
crontab -l
```

## 十一、访问地址
1. 加密主站：https://easydowork.cn
2. www域名：https://www.easydowork.cn
3. 后端API地址：https://easydowork.cn/api/xxx
4. 文件访问：https://easydowork.cn/upload/文件名

## 十二、更新记录

### 2026-07-04 前端 UI 更新
**更新内容：** Logo 替换、品牌色调整、底部 footer 更新、资源详情页优化、导入结果 UI 改进、移动端分页适配

**部署方式：** Git 拉取 + 前端构建
```bash
cd /root/LearningResourceLibrary/frontend
git stash                        # 暂存服务器本地修改
git pull origin master           # 拉取最新代码
npm run build                    # 构建前端
```
**涉及提交：** `5e3b68c` Update site branding and footer logo、`65f3435` 解析算法支持七/八/九年级、`67931f7` 导入结果直接替换第二步区域、`f067a82` 导入完成后自动滚动到结果区域、`1eb89b4` 图片上传路径/登录页默认账号/移动端分页适配

### 2026-07-15 前端视觉主题更新与部署故障处理（进行中）

> **重要更正：** 本文第八节中以 `master` 为例的旧更新脚本已不适用。当前线上生产分支统一为 `fix`，远程仓库为 `https://github.com/DavidBlon/EduShare.git`。

#### 代码与构建状态

- 已推送 GitHub 提交：`29da593 feat: 优化前台资料库视觉主题`；
- 本地分支：`fix`；
- 本地前端已执行并通过生产构建：`cd frontend && npm run build`（仅有 Vite 常规 chunk-size 提示）；
- 本地尚未提交的 `frontend/index.html` 和 `frontend/.claude/` 为本地内容，本次没有推送到 GitHub。

#### 本次前端更新内容

- 统一资料库前台为暖白纸张、深青绿、赭黄色的资料馆视觉主题，移除紫色主色；
- 修复资料库搜索按钮在点击、聚焦、按下时闪成紫色的问题，普通、悬停、按下和波纹均使用绿色系；
- 调整资料详情页“查看网盘信息”按钮，使其符合绿色主题；
- 优化资料库、资源卡片、分类导航、Banner 和前台布局的视觉细节；
- 首页“大书/档案卡片”图案只保留在 PC 端；通过 `max-width: 820px` 媒体查询在移动端和小屏平板隐藏，避免它继续出现在搜索框下方；
- 修复页面中异常问号相关的文本/样式问题。

#### 服务器实际部署状态

服务器：`root@156.239.236.13`  
项目目录：`/root/LearningResourceLibrary`  
前端目录：`/root/LearningResourceLibrary/frontend`

服务器此前的前端源码不是当前 GitHub `fix` 版本，并遗留了合并冲突：

1. 第一次 `npm run build` 失败：`Home.vue` 含两个 `<template>`；
2. 已在 `/root/LearningResourceLibrary/frontend` 执行：
   ```bash
   git fetch origin
   git checkout origin/fix -- src/views/front/Home.vue
   ```
   并确认 `Home.vue` 只保留一组 `<template>` / `</template>`；
3. 再次构建失败：`ResourceDetail.vue` 出现 `<<<<<<< Updated upstream`，说明服务器遗留 Git 合并冲突标记；
4. 该构建失败来自服务器旧前端源码，**不是** GitHub 中提交 `29da593` 的问题；本地已验证当前 `fix` 分支能够构建通过。

#### 当前推荐的恢复与发布步骤（待服务器执行）

当前终端位于 `/root/LearningResourceLibrary/frontend` 时，执行：

```bash
# 1. 备份当前有问题的前端源码
 tar -czf ~/frontend-src-backup-$(date +%F-%H%M%S).tar.gz src

# 2. 仅用 GitHub fix 分支替换前端源码
 git fetch origin
 git checkout origin/fix -- src

# 3. 必须无任何输出；若有输出，说明仍存在冲突，不能继续构建
 grep -R -nE '^(<<<<<<<|=======|>>>>>>>)' src

# 4. 重新构建前端
 npm run build

# 5. 构建成功后发布静态文件
 nginx -s reload
```

若从项目根目录 `/root/LearningResourceLibrary` 执行，第 2、3 步中的路径应写为 `frontend/src`。

> **不要再对前端源码执行 `git stash pop`。** 旧的服务器前端改动会重新带回重复模板或冲突标记。服务器自定义的后端 `src/main/resources/application.properties` 需要保留，但 `frontend/src` 应以 `origin/fix` 为准。

#### 完成标准与验证

在服务器 `npm run build` 成功且 `nginx -s reload` 成功之前，本次线上发布仍视为**未完成**。发布成功后：

```bash
# 可选：确认 Nginx 配置正确
nginx -t
```

浏览器使用 `Ctrl + F5` 强制刷新或清空站点缓存后，重点验证：资料库搜索按钮、资料详情“查看网盘信息”按钮，以及移动端首页不显示大书图案。




